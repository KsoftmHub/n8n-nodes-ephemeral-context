{
  "name": "n8n-nodes-ephemeral-context",
  "version": "0.1.0",
  "description": "An n8n node for execution-scoped, in-memory ephemeral storage.",
  "keywords": [
    "n8n-community-node-package"
  ],
  "license": "MIT",
  "author": {
    "name": "Community Developer",
    "email": "dev@example.com"
  },
  "main": "index.js",
  "scripts": {
    "build": "esbuild SessionStore.node.ts --bundle --platform=node --outfile=dist/SessionStore.node.js",
    "dev": "npm run build -- --watch",
    "lint": "tslint -p tsconfig.json -c tslint.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "dist"
  ],
  "n8n": {
    "nodes": [
      "dist/SessionStore.node.js"
    ]
  },
  "devDependencies": {
    "@types/node": "^14.14.31",
    "esbuild": "^0.27.1",
    "n8n-workflow": "^0.116.0",
    "typescript": "^4.2.3"
  }
}
